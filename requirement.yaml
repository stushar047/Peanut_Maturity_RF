name: peanut  # Replace with your desired environment name
channels:
  - defaults
dependencies:
  - matplotlib
  - scikit-learn
  - pandas
  - numpy
  - pillow
  - pip
  - pip:
    - spectral
    - opencv-python
